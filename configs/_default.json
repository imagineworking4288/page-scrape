{
  "comment": "Universal fallback configuration for all sites. Site-specific configs will override these defaults.",
  "markers": {
    "start": {
      "type": "dynamic",
      "strategy": "first-email",
      "comment": "Dynamically find the first email on the page as the starting point"
    },
    "end": {
      "type": "dynamic",
      "strategy": "viewport-height",
      "comment": "Use viewport height to determine the end boundary"
    }
  },
  "scrollBehavior": {
    "scrollDelay": 1000,
    "maxScrolls": 50,
    "scrollAmount": "viewport",
    "comment": "Conservative scroll settings to avoid rate limiting and ensure content loads properly"
  },
  "selectors": {
    "comment": "OPTIONAL performance hints for DOM extraction. Set to null to use auto-detection (recommended).",
    "container": null,
    "profileLink": null,
    "phone": null,
    "email": null,
    "name": null
  },
  "parsing": {
    "emailDomain": null,
    "nameBeforeEmail": true,
    "profileUrlPatterns": ["/agents/", "/profile/", "/realtor/", "/team/", "/broker/", "/member/", "/lawyer/", "/Lawyers/", "/attorney/", "/people/"],
    "filterPersonalEmails": true,
    "comment": "profileUrlPatterns are URL fragments that indicate profile pages."
  },
  "extraction": {
    "method": "universal",
    "strategies": {
      "email": ["mailto", "text", "data-attr", "obfuscated", "encoded-href"],
      "phone": ["tel", "text", "data-attr", "class-hint"],
      "name": ["heading", "class", "profile-link", "emphasis"]
    },
    "comment": "Universal extraction uses multiple strategies in priority order"
  },
  "pagination": {
    "enabled": true,
    "type": "auto",
    "comment": "Auto-detect pagination pattern. Can be overridden with: 'parameter', 'path', 'offset', 'cursor', 'infinite-scroll', or 'none'",
    "discovery": {
      "timeout": 30000,
      "minContactsPerPage": 1,
      "maxPages": 200,
      "comment": "Discovery settings for automatic pagination detection"
    },
    "patterns": {
      "comment": "Optional manual patterns for pagination. Leave null for auto-detection.",
      "urlPattern": null,
      "nextButtonSelector": null,
      "pageParameterName": null,
      "offsetParameterName": null,
      "cursorParameterName": null
    },
    "emptyPageIndicators": [
      "No results found",
      "No agents found",
      "No listings available",
      "No attorneys found",
      "No lawyers found",
      "0 results"
    ],
    "validation": {
      "checkDuplicateContent": true,
      "minEmailsPerPage": 1,
      "comment": "Validation rules to detect when pagination should stop"
    }
  },
  "infiniteScroll": {
    "enabled": false,
    "scrollDelay": 1500,
    "maxScrollAttempts": 50,
    "noNewContentThreshold": 3,
    "scrollStrategy": "viewport",
    "networkIdleTimeout": 5000,
    "minCardIncrement": 1,
    "extractionMethod": "universal",
    "comment": "Settings for infinite scroll detection and extraction. Set enabled: true to use infinite scroll mode."
  },
  "profileVisiting": {
    "enabled": false,
    "navigationTimeout": 30000,
    "extractionDelay": 1000,
    "maxRetries": 2,
    "retryDelay": 2000,
    "skipIfEmailExists": true,
    "matchByName": true,
    "nameSimilarityThreshold": 0.8,
    "comment": "Settings for visiting individual profile pages to extract emails. Enable when emails only appear on profile pages."
  },
  "fallback": {
    "enabled": true,
    "methods": ["select", "pdf"],
    "completenessThreshold": 0.3,
    "comment": "Enable fallback chain when initial extraction has low completeness"
  },
  "validation": {
    "requireEmail": true,
    "requireName": false,
    "minConfidence": "low",
    "filterBusinessEmailsOnly": true,
    "comment": "Validation rules for extracted contacts"
  }
}
